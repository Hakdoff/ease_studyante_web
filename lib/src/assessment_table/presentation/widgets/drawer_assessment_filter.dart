import 'package:flutter/material.dart';

class DrawerAssessmentFilter extends StatelessWidget {
  const DrawerAssessmentFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Drawer(child: Container());
  }
}
